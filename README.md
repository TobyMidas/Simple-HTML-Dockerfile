# Simple-HTML-Dockerfile
A Simple Dockerfile command which runs a html app and exposes to port 80 for visibility. 

Create a Docker Image with the Docker build command.
Run Docker and expose to port 80.
View your app.
